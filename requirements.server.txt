# AMMO Sessions Server requirements.
# Only packages NOT already provided by vLLM or requirements.txt
#
# vLLM provides: fastapi[standard], uvicorn, httpx, websockets, starlette,
#   pydantic, numpy, torch, triton, mcp, requests, pyyaml, einops, ninja,
#   packaging, typing_extensions, filelock, watchfiles, jsonschema
# requirements.txt provides: numpy, pytest, jsonschema

# S3 session persistence and artifact storage (s3_storage.py, session_state.py)
boto3==1.42.60

# Async test support
pytest-asyncio==1.3.0

# Fork access-token encryption at rest (fork_token_crypto.py)
cryptography==46.0.3
